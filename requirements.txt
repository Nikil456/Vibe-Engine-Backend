# Python 3.11+
pandas>=2.1.0
pydantic>=2.5.0
torch>=2.1.0
transformers>=4.36.0
tqdm>=4.66.0
scikit-learn>=1.3.0
numpy>=1.26.0
fastapi>=0.115.0
uvicorn[standard]>=0.32.0

# Optional: faster CPU inference for zero-shot (uncomment if desired)
# optimum[onnxruntime]  # optional ONNX path — not required for baseline
